dependencies {
    api project(':codion-common-core')
    api project(':codion-common-model')

    api project(':codion-swing-common-model')

    api(libs.ikonli.core)
    implementation(libs.ikonli.swing)

    testImplementation(libs.ikonli.foundation.pack)
}

ext {
    testAddModules = ['org.kordamp.ikonli.foundation']
    testAddReads = ['is.codion.swing.common.ui': 'org.kordamp.ikonli.foundation']
}

compileTestJava {
    moduleOptions {
        addModules = testAddModules
        addReads = testAddReads
    }
}

test {
    moduleOptions {
        addModules = testAddModules
        addReads = testAddReads
    }
}