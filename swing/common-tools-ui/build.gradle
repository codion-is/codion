dependencies {
    api project(':codion-common-core')
    api project(':codion-common-model')

    api project(':codion-swing-common-tools')
    implementation project(':codion-swing-common-ui')
}

ext {
    testAddModules = ['dev.codion.common.core,dev.codion.common.model']
    testAddReads = ['dev.codion.swing.common.tools.ui': 'dev.codion.common.core,dev.codion.common.model']
}

compileTestJava {
    moduleOptions {
        addModules = testAddModules
        addReads = testAddReads
    }
}

test {
    moduleOptions {
        addModules = testAddModules
        addReads = testAddReads
    }
}