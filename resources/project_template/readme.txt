Replace "project_name" (without the quotation marks) in build.xml and build.properties with the actual project name
Configure the junit.db.* in build.properties according to your environment
Edit the jar.* properties in user.properties