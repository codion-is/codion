[#_entityconnection]
= EntityConnection
:dir-tutorials: ../tutorials
:dir-chinook-source: ../../../../../demos/chinook/src/main/java
:url-javadoc: link:../api
:url-entity-connection: {url-javadoc}{framework-db}/is/codion/framework/db/EntityConnection.html

Codion’s {url-entity-connection}[EntityConnection] is the primary interface for executing database operations — including querying, modifying, transaction control, calling procedures and functions and filling reports. It exposes a small, explicit API for working with Entity instances and makes no assumptions about your database engine or schema design.

Codion’s database layer is intentionally minimal. It does not perform SQL joins, nor does it rely on DB-specific features — except where needed for primary key generation via <<manual.adoc#_generator, Generator>> strategies.

Instead, it gives you predictable, queryable access to individual Entity objects and their associated foreign keys — controlled through a feature called reference depth.

The <<{dir-tutorials}/chinook/chinook.adoc#_domain_model, Chinook domain model>> is used in the examples below.

[#_entity_connection_selecting]
== Selecting

By default, when you select a row using {url-entity-connection}[EntityConnection] you receive an Entity instance along with a single level of foreign key references, that is a so-called reference depth of one.
This means that selecting a track you get all the entities referenced via foreign keys as well.

The reference depth can be configured on a foreign key basis when defining entities.

A depth larger than one is a budget for the whole subtree, not merely a limit on the foreign key it is specified for: the referenced entity is fetched along with its own foreign key references, to the same depth, whatever depth those specify.
This is what makes a depth of two fetch the references of the referenced entity, since those typically use the default depth of one, which on its own would stop the fetching.

A depth of zero is the exception, and is absolute: such a foreign key is never populated automatically, not even when it is reached via a foreign key specifying a greater depth.
This is how a foreign key is kept from being fetched at all, whether to keep a child from fetching its parent back, to leave an expensive reference alone or to break a reference cycle.
It can still be fetched by asking for it explicitly when selecting.

A negative reference depth means no limit with the whole dependency graph fetched.
This limiting of foreign key reference depth can be turned off, meaning the full reference graph is always fetched — including foreign keys specifying a depth of zero — via a system property:

[source]
----
codion.db.limitReferenceDepth=false
----

or the {url-javadoc}{framework-db-local}/is/codion/framework/db/local/LocalEntityConnection.html#LIMIT_REFERENCE_DEPTH[LocalEntityConnection.LIMIT_REFERENCE_DEPTH] configuration value:

[source,java,]
----
LocalEntityConnection.LIMIT_REFERENCE_DEPTH.set(false);
----

or on a connection instance via {url-javadoc}{framework-db-local}/is/codion/framework/db/local/LocalEntityConnection.html#limitReferenceDepth(boolean)[limitReferenceDepth(boolean limitReferenceDepth)]

[source,java,]
----
connection.limitReferenceDepth(false);
----

You can specify that the foreign key should never be populated automatically by using a reference depth of 0.
Here an invoice line does not fetch the invoice it belongs to, which is already at hand wherever invoice lines are being displayed.

[source,java,indent=0]
----
include::{dir-chinook-source}/is/codion/demos/chinook/domain/ChinookImpl.java[tags=referenceDepth0]
----

Or you can specify that the foreign key should be populated along with one more level by using a reference depth of 2.
Here a track fetches its album along with the album's artist.

[source,java,indent=0]
----
include::{dir-chinook-source}/is/codion/demos/chinook/domain/ChinookImpl.java[tags=referenceDepth2]
----

Note the `include()` in the example above, which restricts the attributes fetched for the referenced entity.
It doubles as a way to prune the fetching, since only the foreign keys among the included attributes are populated — here the album's cover, a potentially large blob, is left behind, while its artist is included so that the reference depth of 2 has something to reach.

[source,java,indent=0]
----
include::{dir-chinook-source}/is/codion/demos/chinook/manual/EntityConnectionDemo.java[tags=referenceDepthEntity]
----

The reference depth can also be configured on a query basis, either for the whole query or one or more foreign keys.
Both apply wherever the foreign keys occur while fetching the referenced entities, not merely to the entities being selected, and a depth specified for a single foreign key overrides both the depth that foreign key specifies and the depth specified for the query as a whole.
This is also how a foreign key specifying a depth of 0 is fetched when it is needed.

[source,java,indent=0]
----
include::{dir-chinook-source}/is/codion/demos/chinook/manual/EntityConnectionDemo.java[tags=referenceDepthCondition]
----

[source,java,indent=0]
----
include::{dir-chinook-source}/is/codion/demos/chinook/manual/EntityConnectionDemo.java[tags=referenceDepthConditionForeignKey]
----

=== Reference Depth Values

* 0 – Never fetch, unless asked for explicitly when selecting
* 1 (default) – Fetch directly referenced foreign key entities
* N – Fetch up to N levels deep
* -1 – Fetch entire reference graph (no limit)

=== The N+1 problem

Selecting tracks performs four queries (track + album, mediatype and genre), but that number of queries is the same whether you select one or a thousand tracks.

=== Selecting entities

{url-entity-connection}#select(is.codion.framework.domain.entity.condition.Condition)[select(Condition condition)]

{url-entity-connection}#select(is.codion.framework.db.EntityConnection.Select)[select(Select select)]

{url-entity-connection}#selectSingle(is.codion.framework.domain.entity.condition.Condition)[selectSingle(Condition condition)]

{url-entity-connection}#selectSingle(is.codion.framework.db.EntityConnection.Select)[selectSingle(Select select)]

[source,java,indent=0]
----
include::{dir-chinook-source}/is/codion/demos/chinook/manual/EntityConnectionDemo.java[tags=select]
----

{url-entity-connection}#select(is.codion.framework.domain.entity.Entity.Key)[select(Key key)]

[source,java,indent=0]
----
include::{dir-chinook-source}/is/codion/demos/chinook/manual/EntityConnectionDemo.java[tags=selectKey]
----

{url-entity-connection}#select(java.util.Collection)[select(Collection<Key> keys)]

[source,java,indent=0]
----
include::{dir-chinook-source}/is/codion/demos/chinook/manual/EntityConnectionDemo.java[tags=selectKeys]
----

=== Selecting values

For selecting the values of a single column.

{url-entity-connection}#select(is.codion.framework.domain.entity.attribute.Column)[select(Column<T> column)]

{url-entity-connection}#select(is.codion.framework.domain.entity.attribute.Column,is.codion.framework.domain.entity.condition.Condition)[select(Column<T> column, Condition condition)]

{url-entity-connection}#select(is.codion.framework.domain.entity.attribute.Column,is.codion.framework.db.EntityConnection.Select)[select(Column<T> column, Select select)]

[source,java,indent=0]
----
include::{dir-chinook-source}/is/codion/demos/chinook/manual/EntityConnectionDemo.java[tags=selectValues]
----

=== iterator

For iterating over a result set instead of loading it entirely into memory. This is useful when processing large result sets or when memory is constrained.

IMPORTANT: When using remote connections, each call to `hasNext()` and `next()` involves a network round-trip. For large result sets, consider using `select()` instead to load entities in a single batch. Iterators over remote connections that remain idle for longer than the configured timeout (`codion.db.remote.iteratorTimeout`, default 5 minutes) are automatically closed server-side.

NOTE: `iterator()` is not supported on HTTP based connections, and throws `UnsupportedOperationException`.

{url-entity-connection}#iterator(is.codion.framework.domain.entity.condition.Condition)[iterator(Condition condition)]

{url-entity-connection}#iterator(is.codion.framework.db.EntityConnection.Select)[iterator(Select select)]

[source,java,indent=0]
----
include::{dir-chinook-source}/is/codion/demos/chinook/manual/EntityConnectionDemo.java[tags=iterator]
----

=== dependencies

For selecting entities that depend on a set of entities via foreign keys.

{url-entity-connection}#dependencies(java.util.Collection)[dependencies(Collection<Entity> entities)]

[source,java,indent=0]
----
include::{dir-chinook-source}/is/codion/demos/chinook/manual/EntityConnectionDemo.java[tags=dependencies]
----

=== count

For selecting the row count given a count condition.

{url-entity-connection}#count(is.codion.framework.db.EntityConnection.Count)[count(Count count)]

[source,java,indent=0]
----
include::{dir-chinook-source}/is/codion/demos/chinook/manual/EntityConnectionDemo.java[tags=count]
----

== Modifying

=== insert

For inserting rows.

** {url-entity-connection}#insert(is.codion.framework.domain.entity.Entity)[insert(Entity entity)]

** {url-entity-connection}#insertSelect(is.codion.framework.domain.entity.Entity)[insertSelect(Entity entity)]

** {url-entity-connection}#insert(java.util.Collection)[insert(Collection<Entity> entities)]

** {url-entity-connection}#insertSelect(java.util.Collection)[insertSelect(Collection<Entity> entities)]

[source,java,indent=0]
----
include::{dir-chinook-source}/is/codion/demos/chinook/manual/EntityConnectionDemo.java[tags=insert]
----

=== update

For updating one or more entity instances.

IMPORTANT: These methods throw an exception if any of the entities is unmodified.

** {url-entity-connection}#update(is.codion.framework.domain.entity.Entity)[update(Entity entity)]

** {url-entity-connection}#updateSelect(is.codion.framework.domain.entity.Entity)[updateSelect(Entity entity)]

** {url-entity-connection}#update(java.util.Collection)[update(Collection<Entity> entities)]

** {url-entity-connection}#updateSelect(java.util.Collection)[updateSelect(Collection<Entity> entities)]

[source,java,indent=0]
----
include::{dir-chinook-source}/is/codion/demos/chinook/manual/EntityConnectionDemo.java[tags=updateEntity]
----

[#_optimistic_locking]
==== Optimistic locking

The framework performs optimistic locking during updates using the methods above.
This is done by selecting the entities being updated *FOR UPDATE* (when supported by the underlying database) and comparing all original values to the current row values, throwing an exception if one or more values differ or the row is missing. Optimistic locking is field-based: any difference between original and current values causes an update to fail.

[source,java]
----
entity.set(Album.TITLE, "New Title");
connection.update(entity); // fails if the row has been changed by someone else
----

NOTE: Excluding attributes when selecting entities results in those attributes (lazy loaded ones for example) not being included when optimistic locking is performed on subsequent updates, since optimistic locking relies on the original attribute value being available for making a comparison.

Optimistic locking can be turned off system-wide using a system property:

[source]
----
codion.db.optimisticLocking=false
----

or by using the {url-javadoc}{framework-db-local}/is/codion/framework/db/local/LocalEntityConnection.html#OPTIMISTIC_LOCKING[LocalEntityConnection.OPTIMISTIC_LOCKING] configuration value:

[source,java,]
----
LocalEntityConnection.OPTIMISTIC_LOCKING.set(false);
----

or on a connection instance via {url-javadoc}{framework-db-local}/is/codion/framework/db/local/LocalEntityConnection.html#optimisticLocking(boolean)[optimisticLocking()]:

[source,java,]
----
connection.optimisticLocking(false);
----

or on a per-entity basis via {url-javadoc}{framework-domain}/is/codion/framework/domain/entity/EntityDefinition.Builder.html#optimisticLocking(boolean)[EntityDefinition.Builder.optimisticLocking(false)].

For updating by condition.

** {url-entity-connection}#update(is.codion.framework.db.EntityConnection.Update)[update(Update update)]

[source,java,indent=0]
----
include::{dir-chinook-source}/is/codion/demos/chinook/manual/EntityConnectionDemo.java[tags=updateCondition]
----

=== delete

For deleting existing rows.

** {url-entity-connection}#delete(is.codion.framework.domain.entity.condition.Condition)[delete(Condition condition)]

[source,java,indent=0]
----
include::{dir-chinook-source}/is/codion/demos/chinook/manual/EntityConnectionDemo.java[tags=deleteCondition]
----

** {url-entity-connection}#delete(is.codion.framework.domain.entity.Entity.Key)[delete(Key key)]

** {url-entity-connection}#delete(java.util.Collection)[delete(Collection<Key> keys)]

[source,java,indent=0]
----
include::{dir-chinook-source}/is/codion/demos/chinook/manual/EntityConnectionDemo.java[tags=deleteKey]
----

== Procedures & Functions

* Functions return a single value.
* Procedures perform logic with no return value.
* Both are executed through the same API: `EntityConnection.execute(...)`.

=== Function

** {url-entity-connection}#execute(is.codion.common.db.operation.FunctionType)[execute(FunctionType functionType)]
** {url-entity-connection}#execute(is.codion.common.db.operation.FunctionType,P)[execute(FunctionType functionType, P parameter)]

[source,java,indent=0]
----
include::{dir-chinook-source}/is/codion/demos/chinook/manual/EntityConnectionDemo.java[tags=function]
----

=== Procedure

** {url-entity-connection}#execute(is.codion.common.db.operation.ProcedureType)[execute(ProcedureType procedureType)]
** {url-entity-connection}#execute(is.codion.common.db.operation.ProcedureType,P)[execute(ProcedureType procedureType, P parameter)]

[source,java,indent=0]
----
include::{dir-chinook-source}/is/codion/demos/chinook/manual/EntityConnectionDemo.java[tags=procedure]
----

== Reporting

=== report

** {url-entity-connection}#report(is.codion.common.db.report.ReportType,P)[report(ReportType reportType, P reportParameters)]

[source,java,indent=0]
----
include::{dir-chinook-source}/is/codion/demos/chinook/manual/EntityConnectionDemo.java[tags=report]
----

== Transaction control

=== Transactional

Codion encourages declarative transaction boundaries using lambdas or anonymous classes. This ensures transaction safety (commit/rollback) with minimal boilerplate.

Most use cases are covered by:

* {url-entity-connection}#transaction(is.codion.framework.db.EntityConnection,is.codion.framework.db.EntityConnection.Transactional)[EntityConnection.transaction(EntityConnection, Transactional transactional)] – no return value
* {url-entity-connection}#transaction(is.codion.framework.db.EntityConnection,is.codion.framework.db.EntityConnection.TransactionalResult)[EntityConnection.transaction(EntityConnection, TransactionalResult transactional)] – with return value

These methods perform a commit on success and rollback on failure.

NOTE: If a transaction is already open on the connection, the code is executed within the existing transaction without starting a new one. The outermost caller controls the transaction boundary.

==== Transaction without a result

[source,java,indent=0]
----
include::{dir-chinook-source}/is/codion/demos/chinook/manual/EntityConnectionDemo.java[tags=transactionalLambda]
----

.Same example using an anonymous class
[%collapsible]
====
[source,java,indent=0]
----
include::{dir-chinook-source}/is/codion/demos/chinook/manual/EntityConnectionDemo.java[tags=transactionalAnonymous]
----
====

==== Transaction with a result

[source,java,indent=0]
----
include::{dir-chinook-source}/is/codion/demos/chinook/manual/EntityConnectionDemo.java[tags=transactionalResultLambda]
----

.Same example using an anonymous class
[%collapsible]
====
[source,java,indent=0]
----
include::{dir-chinook-source}/is/codion/demos/chinook/manual/EntityConnectionDemo.java[tags=transactionalResultAnonymous]
----
====

=== Transaction

For a more fine-grained transaction control and the ability to rollback, transactions can be started and ended manually, note that this is more complex and thereby error-prone and should not be used unless the method described above does not work for your use-case.

[source,java,indent=0]
----
include::{dir-chinook-source}/is/codion/demos/chinook/manual/EntityConnectionDemo.java[tags=transaction]
----

== Query cache

{url-entity-connection}#cacheQueries()[cacheQueries()] enables query result caching on the connection until the returned QueryCache is closed.
While active, identical selects return the cached result, which is intended for short-lived, read-only scopes, such as application or model initialization, where the same lookup entities would otherwise be selected repeatedly.

[source,java,indent=0]
----
include::{dir-chinook-source}/is/codion/demos/chinook/manual/EntityConnectionDemo.java[tags=queryCache]
----

A cached result is shared by every cache hit, so it is handed out as an unmodifiable list of immutable entities, along with their foreign key references, preventing one caller from modifying what the next one receives.
Use `entity.copy().mutable()` to obtain a modifiable copy.

Note that the cache is not invalidated by inserts, updates, deletes or transaction rollback, that selects for update always bypass the cache and return mutable entities, and that these scopes do not nest.

== LocalEntityConnection

An EntityConnection implementation based on a direct connection to the database, provides access to the underlying JDBC connection.

== RemoteEntityConnection

An EntityConnection implementation based on an RMI connection.
Requires a server.

== HttpEntityConnection

An EntityConnection implementation based on HTTP.
Requires a server.