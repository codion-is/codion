= EntitySearchModel
:dir-chinook-source: ../../../../../demos/chinook/src/main/java
:url-javadoc: link:../api

The {url-javadoc}{framework-model}/is/codion/framework/model/EntitySearchModel.html[EntitySearchModel] is the model component underlying the {url-javadoc}{swing-framework-ui}/is/codion/swing/framework/ui/component/EntitySearchField.html[EntitySearchField] UI component.
It provides entity search functionality with support for multi-column text searching and entity selection.

== Overview

`EntitySearchModel` provides:

* Multi-column text searching with configurable wildcards
* Result limiting to prevent excessive data retrieval
* Case-sensitive or insensitive search options
* The model component for `EntitySearchField` UI component
* Automatic updates when entities are modified

[source,java]
.Basic search model
----
include::{dir-chinook-source}/is/codion/demos/chinook/manual/FrameworkModelDemo.java[tags=basicSearchModel]
----

== Search Configuration

=== Search Settings

Configure search behavior per column:

[source,java]
.Search settings configuration
----
include::{dir-chinook-source}/is/codion/demos/chinook/manual/FrameworkModelDemo.java[tags=searchSettings]
----

=== Wildcard Strategies

The search model supports different wildcard configurations:

* **Prefix search** (autocomplete style): `wildcardPrefix().set(false)`, `wildcardPostfix().set(true)` - "joh" → "joh%"
* **Contains search**: `wildcardPrefix().set(true)`, `wildcardPostfix().set(true)` - "ohn" → "%ohn%"
* **Exact search**: `wildcardPrefix().set(false)`, `wildcardPostfix().set(false)` - "john" → "john"
* **Multi-word search**: `spaceAsWildcard().set(true)` - "john reyk" → "%john%reyk%"

== Selection Management

The search model holds a single selected entity.
To collect several, a UI component wraps the search field in a multi-value input, which keeps the collected entities itself.

[source,java]
.Search model selection
----
include::{dir-chinook-source}/is/codion/demos/chinook/manual/FrameworkModelDemo.java[tags=searchSelection]
----

== Configuration Properties

.EntitySearchModel Configuration
[cols="3,1,5"]
|===
|Configuration value |Default |Description

|{url-javadoc}{framework-model}/is/codion/framework/model/EntitySearchModel.html#PERSISTENCE_AWARE[EntitySearchModel.PERSISTENCE_AWARE]
|true
|Whether search models react to entity persistence events, replacing the selected entity when updated and clearing it when deleted

|{url-javadoc}{framework-model}/is/codion/framework/model/EntitySearchModel.html#DEFAULT_LIMIT[EntitySearchModel.DEFAULT_LIMIT]
|none
|The default search result limit

|{url-javadoc}{framework-model}/is/codion/framework/model/EntitySearchModel.html#WILDCARD_PREFIX[EntitySearchModel.WILDCARD_PREFIX]
|true
|Whether a wildcard is prepended to the search string by default

|{url-javadoc}{framework-model}/is/codion/framework/model/EntitySearchModel.html#WILDCARD_POSTFIX[EntitySearchModel.WILDCARD_POSTFIX]
|true
|Whether a wildcard is appended to the search string by default

|{url-javadoc}{framework-model}/is/codion/framework/model/EntitySearchModel.html#SPACE_AS_WILDCARD[EntitySearchModel.SPACE_AS_WILDCARD]
|true
|Whether spaces in the search string are replaced with wildcards by default

|{url-javadoc}{framework-model}/is/codion/framework/model/EntitySearchModel.html#CASE_SENSITIVE[EntitySearchModel.CASE_SENSITIVE]
|false
|Whether searching is case-sensitive by default
|===